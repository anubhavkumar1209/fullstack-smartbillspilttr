# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/smartbillsplittr?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Admin@1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration - FIXED
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway Configuration - Disabled
spring.flyway.enabled=false

# JWT Configuration
app.jwtSecret=smartBillSplittrSecretKey2024
app.jwtExpirationInMs=604800000

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Logging Configuration
logging.level.com.smartbillsplittr=DEBUG
logging.level.org.springframework.security=DEBUG
